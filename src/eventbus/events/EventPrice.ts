import { TType } from '../type/Type';
import BaseEvent from './BaseEvent';

class EventPrice extends BaseEvent {
  // protected id = 'startPrice';
  // private static current: EventPrice = new EventPrice();
  // protected constructor() {
  //   super();
  // }
  // public subscribe<T extends TType>(callback: (data: T) => void): void {
  //   // super.subscribe(callback);
  // }
  // public emit<T extends TType>(data: T): void {
  //   super.emit(data);
  // }
}

export default EventPrice;
